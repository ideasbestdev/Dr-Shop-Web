import styled from 'styled-components';
import { Theme } from '../Theme';

interface Props {
    theme: Theme,
}

export const VerificateStyle = styled.section`
    padding: 75px 0px;
    display: flex;
    flex-direction: column;
    align-items: center;
`